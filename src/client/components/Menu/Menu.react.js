import React, { Component, PropTypes } from 'react';
import './Menu.less';

export default
class Menu extends Component {
  constructor(props) {
    super(props);
    this.state = { };
  }

  render() {
    return (
      <div className="menu">
      </div>
    );
  }
}

Menu.propTypes = {
};
Menu.defaultProps = {
};

import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import MachineProperties from '.';

describe('<MachineProperties />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <MachineProperties />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<MachineProperties className="test-class-name" />);
    expect(wrapper).to.have.className('machine-properties');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});

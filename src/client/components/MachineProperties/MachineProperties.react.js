import React, { Component, PropTypes } from 'react';
import './MachineProperties.less';

export default
class MachineProperties extends Component {
  constructor(props) {
    super(props);
    this.state = { };
  }

  render() {
    return (
      <div className="machine-properties">
      </div>
    );
  }
}

MachineProperties.propTypes = {
};
MachineProperties.defaultProps = {
};
